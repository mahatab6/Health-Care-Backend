model Admin {
    id String @id @default(uuid(7))

    name          String
    email         String    @unique
    profilePhoto  String?
    contactNumber String?
    address       String?
    gender        Gender
    isDeleted     Boolean   @default(false)
    deletedAt     DateTime?

    createdAt DateTime @default(now())
    upDatedAt DateTime @updatedAt

    userId String @unique
    user   User   @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: Cascade)

    @@index([email], name: "idx_Admin_Email")
    @@index([isDeleted], name: "idx_Admin_isDeleted")
    @@map("Admin")
}

model SuperAdmin {
    id String @id @default(uuid(7))

    name          String
    email         String    @unique
    profilePhoto  String?
    contactNumber String?
    address       String?
    gender        Gender
    isDeleted     Boolean   @default(false)
    deletedAt     DateTime?

    createdAt DateTime @default(now())
    upDatedAt DateTime @updatedAt

    userId String @unique
    user   User   @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: Cascade)

    @@index([email], name: "idx_SuperAdmin_Email")
    @@index([isDeleted], name: "idx_SuperAdmin_isDeleted")
    @@map("SuperAdmin")
}
